You are implementing a Jira task for this Unity project.

1) Restate the task and acceptance criteria.
2) Identify which files will change (ask the repo via search if needed).
3) Propose a short plan (max 6 steps).
4) Implement with minimal diffs.
5) Summarize changes + manual test steps in Unity.
6) If Jira/Confluence MCP is available, draft a Jira comment with: summary, files, test steps, and commit hash placeholder. Jira Project key is MAT.
